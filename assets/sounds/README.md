# assets/sounds

Place `alert.wav` here — the audio alarm played by `apps/alerts/audio.py`
for critical mcd failures.

It should be a short, loud, repeating alarm tone — something that will
get attention at the helm, not a polite chime.

To test before you have a real alarm tone: copy any wav file here and
rename it `alert.wav`.
