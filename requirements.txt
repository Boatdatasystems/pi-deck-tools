# pi-deck-tools dependencies
# Install on Pi: pip install -r requirements.txt

# Astronomy / celestial navigation
skyfield>=1.48

# PDF generation (sun/moon report)
reportlab>=4.0

# Plotting (used by sun/moon tool)
matplotlib>=3.7

# Data handling (Skyfield Hipparcos catalog loading)
pandas>=2.0

# HTTP (Signal K REST client)
requests>=2.31

# Image handling
Pillow>=10.0

# Spreadsheet-like table widget with per-cell formatting
tksheet>=7.3

# tkinter is part of Python stdlib — ensure python3-tk is installed on the Pi:
# sudo apt install python3-tk

# InfluxDB client for Python
influxdb-client>=1.41

# GRIB weather file reading (passage planning)
# Requires system ecCodes library on the Pi first:
#   sudo apt install libeccodes-dev libeccodes-tools
# Then install Python packages:
#   pip install cfgrib xarray scipy
cfgrib>=0.9.10
xarray>=2023.1
scipy>=1.11

# BLE scanner (apps/ble_scanner.py) — ESP32 boat sensors + Victron devices
# (SmartShunt, MPPT) to SignalK over UDP. Requires the system Bluetooth
# adapter (hci1, see hci1-up.service) to be up before this can connect.
psutil>=5.9
bleak>=3.0
victron-ble>=0.9
